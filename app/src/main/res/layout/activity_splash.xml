<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#F1EEE4"
                android:padding="0dp">

    <!-- Imagen en la parte superior sin márgenes -->
    <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="427dp"
            android:layout_alignParentTop="true"
            android:layout_marginTop="-89dp"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/top_splash"/>

    <!-- Contenedor para el texto cargado a la izquierda -->
    <LinearLayout
            android:id="@+id/linearLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/imageView"
            android:layout_marginTop="32dp"
            android:layout_marginStart="24dp"
            android:orientation="vertical">

        <!-- Título principal -->
        <TextView
                android:id="@+id/textView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="ECOCYCLE"
                android:textColor="#3E5227"
                android:textSize="36sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-condensed" />

        <!-- Subtítulo -->
        <TextView
                android:id="@+id/textView2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Un futuro más verde empieza contigo"
                android:textColor="#3E5227"
                android:textSize="20sp"
                android:lineSpacingExtra="4sp" />
    </LinearLayout>

    <!-- Botón con estilo modificado -->
    <Button
            android:id="@+id/btnStart"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/linearLayout"
            android:layout_marginTop="32dp"
            android:layout_marginStart="24dp"
            android:backgroundTint="#3E5227"
            android:paddingHorizontal="48dp"
            android:paddingVertical="12dp"
            android:text="Comenzar"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="18sp" />
</RelativeLayout>